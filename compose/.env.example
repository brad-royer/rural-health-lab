# Copy this file to compose/.env and fill in real values.
# compose/.env is gitignored — never commit it.
#
# This is a synthetic-data lab, but the Postgres password is still a
# credential for a service exposed on your machine. Use any
# non-trivial value (it does not need to be production-grade).

POSTGRES_PASSWORD=changeme
