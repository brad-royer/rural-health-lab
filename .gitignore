# Secrets
.env
*.tfvars
!example.tfvars
*.pem
*.key

# Terraform / OpenTofu
.terraform/
*.tfstate
*.tfstate.*
crash.log

# Ansible
*.retry

# Synthea generated output (can be large; regenerate from config)
synthea/output/
synthea/*.zip

# Synthea jar cache (downloaded by scripts/generate-synthea.sh; large binary,
# reproducible via SYNTHEA_VERSION pin — not committed)
synthea/.cache/

# Docker / runtime
**/data/
**/volumes/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
